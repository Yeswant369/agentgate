fastapi>=0.141
uvicorn[standard]>=0.34
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
razorpay>=1.4
httpx>=0.28
pydantic-settings>=2.7
pytest>=8.3
hypothesis>=6.100
# Pinned exactly: these two gate CI on style/type decisions, so local and CI
# must run identical versions or formatting drift breaks the build.
ruff==0.16.5
mypy==2.3.1
